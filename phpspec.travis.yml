suites:
  default:
    namespace: EcomDev\CacheKey
    psr4_prefix: EcomDev\CacheKey
    src_path: %paths.config%/src
    spec_path: %paths.config%

formatter.name: pretty

code_coverage:
  format: clover
  output: build/coverage.xml

extensions:
  - PhpSpec\Extension\CodeCoverageExtension
